
@{
    ViewBag.Title = "Voluntários";
}

<h2>Voluntários</h2>

<div class="header">
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Doar", "Doar")</li>
    </ul>
    <h1 class="text-muted">Ações</h1>
</div>

<div>
    <table>
        <tr>
            <th>Entidade</th>
            <th>Categoria</th>
            <th>Ação</th>
            <th>Data / Hora</th>
            <th></th>
        </tr>
        @foreach (var acao in ViewBag.Acoes)
            {
            <tr>
                <td>@acao.IdEntidade</td>
                <td>@acao.IdCategoria</td>
                <td>@acao.Descricao</td>
                <td>@acao.DataHora</td>
            </tr>
        }
    </table>
</div>

<h1 class="text-muted">Doações</h1>

<div>
    <table>
        <tr>
            <th>Voluntario</th>
            <th>Doação</th>
            <th>Entidade</th>
            <th></th>
        </tr>
        @foreach (var doacao in ViewBag.Doacoes)
            {
            <tr>
                <td>@doacao.IdVoluntario</td>
                <td>@doacao.Descricao</td>
                <td>@doacao.IdEntidade</td>
            </tr>
        }
    </table>
</div>